import React from 'react';
import './Homepage.css';
import Banner from '../components/Banner';

const HomePage = () => {
    return (
        <div>
            <Banner />
        </div>
    );
};

export default HomePage;
